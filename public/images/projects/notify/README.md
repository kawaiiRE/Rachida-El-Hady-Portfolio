Notify project assets.

The logo comes from `Notify/Notify/assets/icon.png`.
The background image comes from `Notify/Notify/assets/store/feature-graphic.png`.

Current gallery order:

1. `notify-prev-1-upcoming-reminders.png`
2. `notify-prev-2-new-reminder.png`
3. `notify-prev-3-history-export.png`

Add raw app screenshots to `screenshots-inbox/`. After they are added, they can be reviewed,
renamed as ordered `notify-prev-*` files, moved into this folder, and added to `constants/projects.ts`.
